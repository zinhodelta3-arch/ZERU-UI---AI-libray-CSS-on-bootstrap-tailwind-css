# ZERO UI agent instructions

The canonical project instructions are in `AGENT_ZERO_UI.md`.

This short file exists so tooling that looks for `AGENT.md` can still find the ZERO UI project entry point.
